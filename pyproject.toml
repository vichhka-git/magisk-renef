[project]
name = "magisk-renef"
version = "0.1.0"
description = "Build system for MagiskRenef — auto-packaging renef_server as a Magisk module"
requires-python = ">= 3.11"
dependencies = [
    "requests>=2.31.0",
]
